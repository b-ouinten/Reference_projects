$(document).ready(function() {
  LikedCommentId = <%= @comment.id %>
  LikedComment = $('tr#'+LikedCommentId);
  LikedComment.replaceWith('<%= j render 'proposals/proposal_comment', x: @comment, proposal: @proposal %>');
});