<div class="container">
  <div class="row">
    <div class="col-md-6 offset-md-3">
      <%= form_for(current_user, url: user_path(current_user), html: { method: :put, class: "form-signin mt-3" }) do |f| %>
        <h1 class="h3 mb-3 font-weight-normal text-center">Editer mon profil</h1>
          <div class="form-group">
           <%= f.label "Prénom" %><br />
           <%= f.text_field :first_name, autofocus: true, autocomplete: :first_name, class: "form-control" %>
          </div>
          <div class="form-group">
           <%= f.label "Nom" %> <br />
           <%= f.text_field :last_name, autocomplete: :last_name, class: "form-control" %>
          </div>
            <div class="actions">
           <%= f.submit "Editer mon profil", class: "btn btn-primary" %>
        </div>
      <% end %>
      <br>
      <br>
      <h3>Nouvelle image de profil:</h3>
      <br>
      <%= form_tag user_avatars_path(@user), multipart: true do %>
        <%= file_field_tag :avatar , class: "btn btn-sm btn-outline-primary"%>
        <%= submit_tag "Changer", class: "btn btn-sm btn-outline-primary" %>
      <% end %>
      <br>
      <br>
      <h4>Suppression du compte</h4>
        <p>Êtes vous sûr de vouloir supprimer votre compte? Cette action est irréversible. <%= button_to "Supprimer mon compte", registration_path(current_user), data: { confirm: "Are you sure?" }, method: :delete, class: "btn btn-sm btn-outline-primary" %></p>
      <%= link_to "Retour", :back %>
    </div>
  </div>
</div>